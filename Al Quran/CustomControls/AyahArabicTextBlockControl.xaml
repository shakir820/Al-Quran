<UserControl x:Class="Al_Quran.CustomControls.AyahArabicTextBlockControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Al_Quran.CustomControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>

        <TextBlock FontWeight="SemiBold"
                   Foreground="{ThemeResource AlQuranForegroundTextBrush}"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   FontSize="{x:Bind FontSizeVM.FontSize, Mode=OneWay}"
                   LineHeight="{x:Bind FontSizeVM.FontSize, Mode=OneWay, Converter={StaticResource ArabicTextHeightFromFontSizeConverter}}"
                   LineStackingStrategy="BlockLineHeight"
                   Name="textBlock" />

    </Grid>
</UserControl>
